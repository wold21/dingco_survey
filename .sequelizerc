const path = require("path");

module.exports = {
    config: path.resolve("./config", "db_config.json"),
    "models-path": path.resolve("./models"),
    "migrations-path": path.resolve("./migrations"),
};
